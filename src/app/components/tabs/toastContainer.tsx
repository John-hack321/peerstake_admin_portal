'use client'

// we need to a way to build this either into a reusable component or a reusable react hook that we are used to
export default function ToastContainer () {
    return (
        <div></div>
    )
}